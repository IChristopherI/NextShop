import React from 'react';

interface Props {
    className?: string;
}

const CheckoutMain: React.FC<Props> = ({ className }) => {
    return (
        <div>
            <h1>
                dad
            </h1>
        </div>
    );
};

export default CheckoutMain;